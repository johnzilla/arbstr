---
phase: 18-tier-aware-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/router/complexity.rs
  - src/router/selector.rs
  - src/proxy/handlers.rs
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03

must_haves:
  truths:
    - "score_to_max_tier maps score < low to Local, low..=high to Standard, > high to Frontier"
    - "select_candidates filters providers by tier when max_tier is Some"
    - "select_candidates skips tier filtering when max_tier is None"
    - "Existing callers pass None and behavior is unchanged"
    - "When tier filter eliminates all candidates, NoPolicyMatch is returned"
  artifacts:
    - path: "src/router/complexity.rs"
      provides: "score_to_max_tier function"
      contains: "pub fn score_to_max_tier"
    - path: "src/router/selector.rs"
      provides: "Tier-filtered select_candidates"
      contains: "max_tier"
  key_links:
    - from: "src/router/complexity.rs"
      to: "src/config.rs"
      via: "Tier enum import"
      pattern: "use crate::config::Tier"
    - from: "src/router/selector.rs"
      to: "src/config.rs"
      via: "Tier enum for filtering"
      pattern: "provider\\.tier.*max_tier"
---

<objective>
Add tier-aware filtering to the router so providers are selected based on complexity score thresholds.

Purpose: Enable the routing pipeline to restrict provider candidates by tier (local/standard/frontier) based on a complexity score. This is the core routing logic that Phase 19 will wire into the request handler.

Output: `score_to_max_tier` mapping function, `max_tier` parameter on `select_candidates`/`select`, tier filter predicate, updated caller, unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-tier-aware-routing/18-CONTEXT.md

@src/router/selector.rs
@src/router/complexity.rs
@src/config.rs

<interfaces>
<!-- Key types the executor needs -->

From src/config.rs:
```rust
#[derive(Debug, Clone, Copy, Default, PartialEq, Eq, PartialOrd, Ord, Hash, Serialize, Deserialize)]
#[serde(rename_all = "lowercase")]
pub enum Tier {
    Local,
    #[default]
    Standard,
    Frontier,
}

pub struct RoutingConfig {
    pub complexity_threshold_low: f64,  // default 0.4
    pub complexity_threshold_high: f64, // default 0.7
    pub complexity_weights: ComplexityWeightsConfig,
}
```

From src/router/selector.rs:
```rust
pub fn select_candidates(&self, model: &str, policy_name: Option<&str>, prompt: Option<&str>) -> Result<Vec<SelectedProvider>>
pub fn select(&self, model: &str, policy_name: Option<&str>, prompt: Option<&str>) -> Result<SelectedProvider>
```

From src/proxy/handlers.rs (line ~248):
```rust
let candidates = match state.router.select_candidates(&ctx.model, ctx.policy_name.as_deref(), user_prompt) {
```

Tier ordering: Local < Standard < Frontier (Ord derived). `provider.tier <= max_tier` works directly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add score_to_max_tier mapping function</name>
  <files>src/router/complexity.rs</files>
  <read_first>src/router/complexity.rs, src/config.rs</read_first>
  <behavior>
    - score_to_max_tier(0.2, 0.4, 0.7) returns Tier::Local (below low threshold)
    - score_to_max_tier(0.39, 0.4, 0.7) returns Tier::Local (just below low)
    - score_to_max_tier(0.4, 0.4, 0.7) returns Tier::Standard (at low threshold)
    - score_to_max_tier(0.5, 0.4, 0.7) returns Tier::Standard (between thresholds)
    - score_to_max_tier(0.7, 0.4, 0.7) returns Tier::Standard (at high threshold)
    - score_to_max_tier(0.71, 0.4, 0.7) returns Tier::Frontier (above high threshold)
    - score_to_max_tier(1.0, 0.4, 0.7) returns Tier::Frontier (maximum score)
    - score_to_max_tier(0.0, 0.4, 0.7) returns Tier::Local (minimum score)
  </behavior>
  <action>
Add a public function `score_to_max_tier(score: f64, low: f64, high: f64) -> Tier` to `src/router/complexity.rs` (per D-04).

Mapping logic per D-05:
- score < low -> Tier::Local
- low <= score <= high -> Tier::Standard
- score > high -> Tier::Frontier

Add `use crate::config::Tier;` import. Add unit tests covering all boundary conditions listed in the behavior block. The function is a simple free function, no struct needed.

Re-export `score_to_max_tier` from `src/router/mod.rs` alongside the existing `score_complexity` re-export.
  </action>
  <verify>
    <automated>cd /home/john/vault/projects/github.com/arbstr && cargo test router::complexity::tests::test_score_to_max_tier -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "pub fn score_to_max_tier" src/router/complexity.rs
    - grep -q "score_to_max_tier" src/router/mod.rs
    - cargo test router::complexity::tests passes
  </acceptance_criteria>
  <done>score_to_max_tier function exists, boundary tests pass, re-exported from router module</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add max_tier parameter to select_candidates and select</name>
  <files>src/router/selector.rs, src/proxy/handlers.rs</files>
  <read_first>src/router/selector.rs, src/proxy/handlers.rs</read_first>
  <behavior>
    - select_candidates("gpt-4o", None, None, None) returns all matching providers (backward compat)
    - select_candidates("gpt-4o", None, None, Some(Tier::Local)) returns only Local tier providers
    - select_candidates("gpt-4o", None, None, Some(Tier::Standard)) returns Local + Standard tier providers
    - select_candidates("gpt-4o", None, None, Some(Tier::Frontier)) returns all tiers
    - select_candidates with max_tier set and no matching providers returns NoPolicyMatch error
    - select("gpt-4o", None, None, None) returns cheapest provider (unchanged behavior)
    - All existing tests pass with None as the new parameter
  </behavior>
  <action>
Per D-01, D-02, D-03, D-07, D-08, D-09:

1. **Modify `select_candidates` signature** (line ~89 of selector.rs): Add `max_tier: Option<Tier>` as the fourth parameter. Inside the method, AFTER the model filter (line ~99-103) and BEFORE policy constraint application (line ~112), insert a tier filter:
   ```rust
   // Filter by tier when max_tier is set (per D-07)
   if let Some(max_tier) = max_tier {
       candidates.retain(|p| p.tier <= max_tier);
       if candidates.is_empty() {
           return Err(Error::NoPolicyMatch);
       }
   }
   ```
   Per D-09, when `max_tier` is `None`, this block is skipped entirely.

2. **Modify `select` signature** (line ~68): Add `max_tier: Option<Tier>` as the fourth parameter. Pass it through to `select_candidates`.

3. **Update handler caller** (handlers.rs line ~248): Add `None` as the fourth argument to `select_candidates` call. This preserves existing behavior per D-03.

4. **Update ALL existing unit tests** in `selector.rs`: Add `None` as the fourth argument to every `select()` and `select_candidates()` call (approximately 10 call sites in tests).

5. **Add new unit tests** for tier filtering:
   - Create a `tiered_providers()` helper that returns 3 providers: one `Tier::Local` (cheapest), one `Tier::Standard` (mid), one `Tier::Frontier` (expensive), all supporting "gpt-4o".
   - Test: `max_tier=Some(Tier::Local)` returns only the local provider.
   - Test: `max_tier=Some(Tier::Standard)` returns local + standard providers (2 results).
   - Test: `max_tier=Some(Tier::Frontier)` returns all 3 providers.
   - Test: `max_tier=None` returns all 3 providers (no filtering).
   - Test: `max_tier=Some(Tier::Local)` with no local providers returns `Err(Error::NoPolicyMatch)`.
  </action>
  <verify>
    <automated>cd /home/john/vault/projects/github.com/arbstr && cargo test router::selector::tests -- --nocapture && cargo test --test '*' -- --nocapture 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "max_tier: Option<Tier>" src/router/selector.rs
    - grep -q "max_tier" src/proxy/handlers.rs
    - grep -q "p.tier <= max_tier" src/router/selector.rs
    - cargo test passes (all existing + new tests)
  </acceptance_criteria>
  <done>select_candidates and select accept max_tier parameter, tier filtering works, existing callers pass None, all tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config -> router | Threshold values from config file (trusted local input) |
| client -> proxy | max_tier not yet exposed to clients (Phase 19 adds header) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | score_to_max_tier | accept | Function takes f64 thresholds from config (trusted). No user input reaches thresholds in this phase. Phase 19 will validate X-Arbstr-Complexity header values. |
| T-18-02 | Denial of Service | tier filtering | accept | Tier filter is a simple retain() predicate on an already-bounded provider list. No amplification risk. |
</threat_model>

<verification>
1. `cargo test` -- all existing tests pass (backward compatibility)
2. `cargo test router::complexity::tests::test_score_to_max_tier` -- boundary mapping correct
3. `cargo test router::selector::tests` -- tier filtering tests pass
4. `cargo clippy -- -D warnings` -- no new warnings
</verification>

<success_criteria>
- score_to_max_tier correctly maps scores to tiers at all boundary conditions
- select_candidates filters by tier when max_tier is Some, skips when None
- All existing tests pass unchanged (with None parameter added)
- No handler behavior change (Phase 19 wires actual tier values)
</success_criteria>

<output>
After completion, create `.planning/phases/18-tier-aware-routing/18-01-SUMMARY.md`
</output>
