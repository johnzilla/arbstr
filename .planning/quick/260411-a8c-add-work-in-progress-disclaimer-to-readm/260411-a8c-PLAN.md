---
phase: quick
plan: 260411-a8c
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/index.html
autonomous: true
requirements: []
must_haves:
  truths:
    - "README shows an understated work-in-progress notice near the top"
    - "Landing page shows a subtle early-stage notice in the hero section"
  artifacts:
    - path: "README.md"
      provides: "WIP disclaimer after opening description"
      contains: "active development"
    - path: "docs/index.html"
      provides: "Subtle early-stage notice in hero"
      contains: "active development"
  key_links: []
---

<objective>
Add a simple, understated work-in-progress disclaimer to both README.md and the landing page.

Purpose: Signal honestly that arbstr is early-stage software without being alarmist.
Output: Updated README.md and docs/index.html with subtle notices.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md
@docs/index.html
@docs/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WIP disclaimer to README and landing page</name>
  <files>README.md, docs/index.html</files>
  <action>
1. In README.md, insert a one-line notice on line 3 (after the tagline, before the description paragraph). Use a blockquote format:

> **Early-stage software** — under active development. APIs and configuration may change.

Keep it to one line. No emoji, no scary language, no "WARNING" or "CAUTION". Just factual.

2. In docs/index.html, add a small notice element inside the hero `.container` div, immediately after the `.sub-tagline` paragraph and before the `.cta-buttons` div. Use a simple paragraph:

```html
<p class="wip-notice">Early-stage software under active development</p>
```

Style it inline or add to style.css: muted color (#888 or similar), smaller font size (0.85rem), no border or background — just understated text. Add a small top margin (0.75rem) so it sits naturally between the sub-tagline and CTA buttons.

Add to docs/style.css at the end:

```css
.wip-notice {
  color: #888;
  font-size: 0.85rem;
  margin-top: 0.75rem;
}
```
  </action>
  <verify>
    <automated>grep -q "active development" README.md && grep -q "wip-notice" docs/index.html && grep -q "wip-notice" docs/style.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>README.md has an understated blockquote disclaimer after the tagline. Landing page hero has a subtle muted-text notice. Both mention "active development" without being alarmist.</done>
</task>

</tasks>

<verification>
- README.md contains a single-line understated disclaimer near the top
- docs/index.html hero section contains a subtle notice element
- docs/style.css has the .wip-notice rule
- No alarming language ("WARNING", "DANGER", "UNSTABLE") present
</verification>

<success_criteria>
Both README and landing page communicate early-stage status in an honest, understated way that matches the project's tone.
</success_criteria>

<output>
After completion, create `.planning/quick/260411-a8c-add-work-in-progress-disclaimer-to-readm/260411-a8c-SUMMARY.md`
</output>
