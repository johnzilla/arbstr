---
phase: 20-routing-observability
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - src/proxy/stats.rs
  - src/storage/stats.rs
  - tests/stats.rs
autonomous: true
requirements: [OBS-04]

must_haves:
  truths:
    - "GET /v1/stats?group_by=tier returns per-tier cost and performance breakdown"
    - "Rows with NULL tier are grouped as 'unknown' in the tier breakdown"
    - "Tier breakdown includes counts, costs, and performance sections matching the model breakdown shape"
  artifacts:
    - path: "src/storage/stats.rs"
      provides: "TierRow struct and query_grouped_by_tier function"
      contains: "query_grouped_by_tier"
    - path: "src/proxy/stats.rs"
      provides: "group_by=tier handling in stats_handler"
      contains: "\"tier\""
    - path: "tests/stats.rs"
      provides: "Integration tests for group_by=tier"
      contains: "group_by=tier"
  key_links:
    - from: "src/proxy/stats.rs"
      to: "src/storage/stats.rs"
      via: "stats_handler calls query_grouped_by_tier"
      pattern: "query_grouped_by_tier"
    - from: "src/proxy/stats.rs"
      to: "StatsResponse"
      via: "tiers field added to response"
      pattern: "tiers"
---

<objective>
Add group_by=tier support to GET /v1/stats so operators can see per-tier cost and performance breakdown.

Purpose: Enable cost analysis by routing tier (local vs standard vs frontier) to validate that complexity routing is saving sats.
Output: TierRow query, stats handler tier grouping, integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-routing-observability/20-CONTEXT.md
@.planning/phases/20-routing-observability/20-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: DB now has complexity_score REAL and tier TEXT columns -->

From src/storage/stats.rs (existing pattern to follow):
```rust
#[derive(sqlx::FromRow)]
pub struct ModelRow {
    pub model: String,
    pub total_requests: i64,
    pub total_cost_sats: f64,
    pub total_input_tokens: f64,
    pub total_output_tokens: f64,
    pub avg_latency_ms: f64,
    pub success_count: i64,
    pub error_count: i64,
    pub streaming_count: i64,
}

pub async fn query_grouped_by_model(pool, since, until, provider) -> Result<Vec<ModelRow>, sqlx::Error>
```

From src/proxy/stats.rs (existing pattern to follow):
```rust
pub struct StatsQuery {
    pub group_by: Option<String>,  // Currently: "model" only
}

pub struct StatsResponse {
    // ...existing fields...
    pub models: Option<serde_json::Value>,  // Present when group_by=model
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TierRow query, stats handler group_by=tier, and integration tests</name>
  <files>
    src/storage/stats.rs,
    src/proxy/stats.rs,
    tests/stats.rs
  </files>
  <read_first>
    src/storage/stats.rs,
    src/proxy/stats.rs,
    tests/stats.rs
  </read_first>
  <behavior>
    - Test 1: GET /v1/stats?group_by=tier returns 200 with a "tiers" object in the response
    - Test 2: Tiers object contains per-tier entries with counts/costs/performance shape matching model breakdown
    - Test 3: Rows with NULL tier grouped under "unknown" key
    - Test 4: group_by=tier works with model and provider filters
    - Test 5: group_by=tier with time range filters works correctly
  </behavior>
  <action>
1. **TierRow in storage/stats.rs** (per D-09): Create `TierRow` struct identical to `ModelRow` but with `tier: String` instead of `model: String`. Derive `sqlx::FromRow`.

2. **query_grouped_by_tier** in `src/storage/stats.rs`: Clone the `query_grouped_by_model` pattern but:
   - SELECT `COALESCE(tier, 'unknown') as tier` instead of `model` (per D-10)
   - GROUP BY `COALESCE(tier, 'unknown')`
   - Accept optional `model` filter (instead of `provider` filter -- tier query benefits from model filtering)
   - Also accept optional `provider` filter for consistency
   Signature: `pub async fn query_grouped_by_tier(pool, since, until, model: Option<&str>, provider: Option<&str>) -> Result<Vec<TierRow>, sqlx::Error>`

3. **Stats handler** in `src/proxy/stats.rs`:
   - Expand the `group_by` validation to accept `"model"` or `"tier"` (change the error message to `"Supported: 'model', 'tier'"`)
   - Add a `tiers` field to `StatsResponse`: `pub tiers: Option<serde_json::Value>` with `#[serde(skip_serializing_if = "Option::is_none")]`
   - When `group_by=tier`: call `query_grouped_by_tier`, build a JSON map keyed by tier name with the same counts/costs/performance shape as model breakdown. Include all 3 known tiers (local, standard, frontier) plus "unknown" if present. Use `tier_row_to_json` helper mirroring `model_row_to_json`.
   - Populate `tiers` field in the response (None when not group_by=tier).

4. **Integration tests** in `tests/stats.rs`: Add tests at the end of the file:
   - `test_stats_group_by_tier_empty` -- no requests, returns tiers object with zeroed entries for configured tiers
   - `test_stats_group_by_tier_with_data` -- insert test rows with different tier values via direct SQL INSERT (including complexity_score and tier columns), verify the tier breakdown sums correctly
   - `test_stats_group_by_tier_null_tier` -- insert rows without tier column set, verify they appear under "unknown"
   - `test_stats_group_by_invalid` -- group_by=invalid returns 400 with message mentioning "model" and "tier"
  </action>
  <verify>
    <automated>cd /home/john/vault/projects/github.com/arbstr && cargo test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - TierRow struct exists in src/storage/stats.rs
    - query_grouped_by_tier function exists with COALESCE(tier, 'unknown')
    - StatsResponse has tiers field
    - group_by validation accepts "model" and "tier"
    - Integration tests for group_by=tier pass
    - cargo test full suite passes
  </acceptance_criteria>
  <done>GET /v1/stats?group_by=tier returns per-tier breakdown with counts/costs/performance, NULL tiers grouped as "unknown", validation accepts both "model" and "tier", full test suite passes including new integration tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> group_by param | User-supplied query parameter, validated against allowlist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Injection | group_by parameter | mitigate | Validated against exact string match allowlist ("model", "tier"). Never interpolated into SQL. |
| T-20-06 | Information Disclosure | tier breakdown | accept | Aggregate stats by tier reveal routing distribution, same sensitivity level as existing model breakdown. No PII. |
</threat_model>

<verification>
1. `cargo test` -- full suite green including new tier stats tests
2. `cargo clippy -- -D warnings` -- clean
3. `cargo run -- serve --mock` and `curl -s 'http://localhost:8080/v1/stats?group_by=tier' | jq` -- returns tiers object with per-tier breakdown
</verification>

<success_criteria>
- GET /v1/stats?group_by=tier returns JSON with tiers object containing per-tier counts, costs, and performance
- NULL tier rows grouped under "unknown"
- group_by validation accepts "model" and "tier", rejects others
- Integration tests cover empty, populated, and null-tier scenarios
- No regression in existing stats tests
</success_criteria>

<output>
After completion, create `.planning/phases/20-routing-observability/20-02-SUMMARY.md`
</output>
