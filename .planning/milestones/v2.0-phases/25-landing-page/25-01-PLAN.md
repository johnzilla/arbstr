---
phase: 25-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/index.html
  - docs/style.css
  - docs/CNAME
autonomous: true
requirements:
  - DEPLOY-04
must_haves:
  truths:
    - "arbstr.com loads with hero section showing 'NiceHash for AI inference' tagline"
    - "How it works section explains the 4-step flow: app -> arbstr -> cheapest provider -> Bitcoin settlement"
    - "Anti-token manifesto section contains 'No tokens. No staking. No governance theater. Just sats.'"
    - "Getting started section shows 3-step terminal snippet: clone, configure, compose up"
    - "Footer links to GitHub repo"
    - "Page uses dark theme with Bitcoin orange (#f7931a) accent"
  artifacts:
    - path: "docs/index.html"
      provides: "Complete landing page with 4 sections, footer, meta tags"
      min_lines: 100
    - path: "docs/style.css"
      provides: "Dark theme styles, responsive layout, terminal code blocks"
      min_lines: 80
    - path: "docs/CNAME"
      provides: "Custom domain configuration for GitHub Pages"
      contains: "arbstr.com"
  key_links:
    - from: "docs/index.html"
      to: "docs/style.css"
      via: "link rel stylesheet"
      pattern: "style\\.css"
    - from: "docs/index.html"
      to: "https://github.com/johnzilla/arbstr-node"
      via: "getting started clone URL"
      pattern: "github\\.com/johnzilla/arbstr-node"
    - from: "docs/index.html"
      to: "https://github.com/johnzilla/arbstr"
      via: "source code link in footer"
      pattern: "github\\.com/johnzilla/arbstr"
---

<objective>
Create the arbstr.com landing page as static HTML + CSS in /docs for GitHub Pages deployment.

Purpose: Communicate arbstr's marketplace vision (NiceHash for AI inference), explain Bitcoin-native settlement, and onboard developers with a quick start guide. This is the public face of the project.

Output: docs/index.html, docs/style.css, docs/CNAME — ready for GitHub Pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-landing-page/25-CONTEXT.md

<interfaces>
<!-- No existing code interfaces — greenfield static page. -->
<!-- Reference material for getting started content: -->

From arbstr-node docker-compose.yml:
- Services: lnd, mint, vault, core
- Core image: ghcr.io/johnzilla/arbstr:latest
- Core port: 8080

From arbstr-node .env.example:
- VAULT_ADMIN_TOKEN (required, min 32 chars)
- VAULT_INTERNAL_TOKEN (shared between core and vault)
- MINT_PRIVATE_KEY (auto-generated if not set)
- RUST_LOG (default: arbstr=info)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create index.html with all page sections and CNAME</name>
  <read_first>
    - .planning/phases/25-landing-page/25-CONTEXT.md (decisions D-01 through D-12)
    - .planning/PROJECT.md (core value, anti-token positioning)
  </read_first>
  <files>docs/index.html, docs/CNAME</files>
  <action>
Create the docs/ directory. Write docs/CNAME containing exactly `arbstr.com` (no trailing newline preferred but not critical).

Write docs/index.html as a single semantic HTML5 page with NO JavaScript. Structure per D-06 and D-07:

**Head section:**
- Charset UTF-8, viewport meta for mobile
- Title: "arbstr — NiceHash for AI inference"
- Meta description: "Open-source inference marketplace. Route AI compute to the cheapest provider. Settle in bitcoin."
- Open Graph tags: og:title, og:description, og:type (website), og:url (https://arbstr.com)
- Link to style.css (per D-01, D-02 — no Tailwind, no build step)
- Preconnect to fonts.googleapis.com and fonts.gstatic.com for Inter and JetBrains Mono (per D-09)
- Google Fonts link for Inter (400,600) and JetBrains Mono (400)

**Body — Section 1: Hero** (id="hero")
- h1: "arbstr"
- Tagline paragraph: "NiceHash for AI inference"
- Sub-tagline: "Route AI compute to the cheapest qualified provider. Settle in bitcoin over Lightning."
- Two CTA buttons: "Get Started" (anchor to #getting-started) and "GitHub" (link to https://github.com/johnzilla/arbstr)

**Body — Section 2: How It Works** (id="how-it-works")
- h2: "How It Works"
- 4 steps displayed as a horizontal flow (CSS handles layout, semantic HTML here):
  1. "Your App" — "Any OpenAI-compatible client sends a request"
  2. "arbstr routes" — "Scores complexity, matches policy, picks cheapest provider"
  3. "Provider responds" — "Inference runs on the optimal node (local, cloud, or mesh)"
  4. "Bitcoin settles" — "Actual cost settled in sats via Lightning/Cashu — no tokens, no subscriptions"
- Each step: div with a step number, title, and description

**Body — Section 3: Anti-Token Manifesto** (id="manifesto")
- h2: "The Manifesto"
- Lead paragraph (large text): "No tokens. No staking. No governance theater. Just sats."
- Supporting copy (2-3 sentences): "Every other AI marketplace invents a token. We use bitcoin — the money that already works. arbstr settles inference costs in satoshis over Lightning. Providers get paid. Buyers pay fair price. No middleman token skimming value."

**Body — Section 4: Getting Started** (id="getting-started")
- h2: "Get Started"
- Intro text: "Run your own inference marketplace in under a minute."
- Terminal-style code block (pre > code) with 3 steps per D-11:
  ```
  git clone https://github.com/johnzilla/arbstr-node && cd arbstr-node
  cp .env.example .env  # edit with your secrets
  docker compose up
  ```
- Second code block showing a test curl:
  ```
  curl http://localhost:8080/v1/chat/completions \
    -H "Content-Type: application/json" \
    -H "Authorization: Bearer your-agent-token" \
    -d '{"model": "gpt-4o", "messages": [{"role": "user", "content": "Hello"}]}'
  ```
- Brief note per D-12: "See the full documentation on GitHub" linking to https://github.com/johnzilla/arbstr-node#readme

**Body — Footer** (per D-07)
- Project description: "arbstr is an open-source inference marketplace routing AI compute to the cheapest provider with Bitcoin settlement."
- Links: GitHub (https://github.com/johnzilla/arbstr), MIT License
- Copyright: current year, "arbstr contributors"

Use semantic HTML elements: header, main, section, footer. Use class names that map to style.css (e.g., .hero, .how-it-works, .step, .manifesto, .getting-started, .terminal, .footer).
  </action>
  <verify>
    <automated>test -f docs/index.html && test -f docs/CNAME && grep -q "arbstr.com" docs/CNAME && grep -q "NiceHash for AI inference" docs/index.html && grep -q "No tokens" docs/index.html && grep -q "docker compose up" docs/index.html && grep -q "github.com/johnzilla/arbstr" docs/index.html && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - docs/index.html exists with 4 sections: hero, how-it-works, manifesto, getting-started
    - grep "NiceHash for AI inference" docs/index.html returns a match
    - grep "No tokens. No staking. No governance theater. Just sats." docs/index.html returns a match
    - grep "docker compose up" docs/index.html returns a match
    - grep "github.com/johnzilla/arbstr-node" docs/index.html returns a match (getting started link)
    - grep "github.com/johnzilla/arbstr" docs/index.html returns a match (footer/source link)
    - docs/CNAME contains "arbstr.com"
    - No script tags in index.html (per D-01: no JavaScript)
    - Footer contains GitHub link and license info
    - Open Graph meta tags present (og:title, og:description)
  </acceptance_criteria>
  <done>index.html contains all 4 sections with correct content, CNAME points to arbstr.com, no JavaScript anywhere</done>
</task>

<task type="auto">
  <name>Task 2: Create style.css with dark theme, responsive layout, and terminal code blocks</name>
  <read_first>
    - docs/index.html (the HTML structure from Task 1 — needed to write matching selectors)
    - .planning/phases/25-landing-page/25-CONTEXT.md (D-08 colors, D-09 typography, D-10 terminal style)
  </read_first>
  <files>docs/style.css</files>
  <action>
Write docs/style.css implementing the full visual design per D-08, D-09, D-10.

**CSS Reset and Base:**
- Box-sizing border-box on all elements
- Body: background #0a0a0a, color #e5e5e5, font-family 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif
- Smooth scrolling on html (scroll-behavior: smooth) for anchor navigation
- Max-width container (900px) centered with auto margins, padding 0 1.5rem

**Typography (per D-09):**
- h1: large (3rem+), font-weight 600, color white
- h2: 2rem, font-weight 600, color white
- Body text: 1.125rem, line-height 1.7, color #e5e5e5
- Code/pre: font-family 'JetBrains Mono', 'Fira Code', 'Cascadia Code', monospace

**Color palette (per D-08):**
- Background: #0a0a0a
- Text: #e5e5e5
- Accent (Bitcoin orange): #f7931a — used for CTA buttons, step numbers, h2 decorations
- Code block background: #1a1a2e
- Subtle borders/dividers: #222

**Hero section:**
- Centered text, generous vertical padding (6rem top, 4rem bottom)
- h1 arbstr: large, white
- Tagline: accent color #f7931a, font-size 1.5rem
- Sub-tagline: #e5e5e5, slightly smaller
- CTA buttons: inline-flex, side by side with gap
  - "Get Started": background #f7931a, color #0a0a0a, bold, rounded corners, padding 0.75rem 2rem
  - "GitHub": border 1px solid #f7931a, color #f7931a, transparent background, same sizing
  - Hover states: slight brightness change or background shift

**How It Works section:**
- 4 steps in a CSS grid: 4 columns on desktop (min-width: 768px), 1 column on mobile
- Each step: step number in accent color (large, bold), title in white, description in #e5e5e5
- Optional: connecting arrows between steps on desktop using CSS pseudo-elements (::after with content "->")
- Vertical padding 4rem

**Manifesto section:**
- Centered text, generous padding
- Lead quote: font-size 1.75rem, color #f7931a, font-weight 600
- Supporting text: standard body size, max-width 600px centered

**Getting Started section:**
- Terminal-style code blocks per D-10:
  - Background: #1a1a2e
  - Border-radius: 8px
  - Padding: 1.5rem
  - Top bar decoration: three colored dots (CSS-only, using ::before pseudo-element with content and colored circles via radial-gradient or border-radius spans)
  - Code text: #e5e5e5, JetBrains Mono
  - Comments (#) in a dimmer color (#888)
  - Overflow-x auto for horizontal scroll on narrow screens
- Intro text and GitHub docs link styled normally

**Footer:**
- Border-top: 1px solid #222
- Padding: 2rem 0
- Muted text color (#888)
- Links in accent color

**Responsive breakpoints:**
- Below 768px: how-it-works grid becomes single column, hero padding reduces, font sizes scale down slightly
- Below 480px: further padding reduction, button stack vertically

**Accessibility:**
- Focus outlines on buttons and links (outline: 2px solid #f7931a with offset)
- Sufficient contrast ratios (light text on dark background exceeds WCAG AA)
- Prefers-reduced-motion: disable smooth scrolling

No CSS animations beyond hover transitions (keep it fast and technical per D-10). No JavaScript.
  </action>
  <verify>
    <automated>test -f docs/style.css && grep -q "#0a0a0a" docs/style.css && grep -q "#f7931a" docs/style.css && grep -q "#1a1a2e" docs/style.css && grep -q "JetBrains Mono" docs/style.css && grep -q "@media" docs/style.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - docs/style.css exists with 80+ lines
    - grep "#0a0a0a" docs/style.css matches (dark background per D-08)
    - grep "#f7931a" docs/style.css matches (Bitcoin orange accent per D-08)
    - grep "#1a1a2e" docs/style.css matches (code block background per D-08)
    - grep "#e5e5e5" docs/style.css matches (text color per D-08)
    - grep "JetBrains Mono" docs/style.css matches (code font per D-09)
    - grep "Inter" docs/style.css matches (body font per D-09)
    - grep "@media" docs/style.css matches (responsive breakpoints present)
    - grep "prefers-reduced-motion" docs/style.css matches (accessibility)
    - No JavaScript in any file under docs/
  </acceptance_criteria>
  <done>style.css implements full dark theme with Bitcoin orange accent, responsive grid layout, terminal-style code blocks, and accessibility focus states</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static files -> browser | GitHub Pages serves HTML/CSS to visitors — no server-side processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Spoofing | docs/CNAME | accept | Custom domain ownership verified via GitHub Pages DNS settings; not in scope of this codebase |
| T-25-02 | Tampering | docs/index.html | accept | Static files served by GitHub Pages CDN with HTTPS; no user input, no forms, no JavaScript |
| T-25-03 | Information Disclosure | docs/index.html | accept | Page is entirely public marketing content; no secrets, no PII, no auth |
| T-25-04 | Denial of Service | GitHub Pages | accept | GitHub Pages has its own DDoS protection; no server-side logic to exploit |

Note: This is a static HTML page with zero JavaScript, no forms, no user input, and no server-side logic. The threat surface is minimal. All content is intentionally public.
</threat_model>

<verification>
1. `ls docs/index.html docs/style.css docs/CNAME` — all three files exist
2. `grep -c "section" docs/index.html` — returns 4+ (4 content sections)
3. `grep "NiceHash for AI inference" docs/index.html` — hero tagline present
4. `grep "No tokens" docs/index.html` — manifesto present
5. `grep "docker compose up" docs/index.html` — getting started present
6. `grep "#f7931a" docs/style.css` — Bitcoin orange accent applied
7. `grep -L "script" docs/index.html` — no script tags (returns filename if no match, empty if match found; verify no JS)
8. Open docs/index.html in browser — dark theme, 4 sections visible, responsive on mobile
</verification>

<success_criteria>
- arbstr.com landing page files exist in docs/ ready for GitHub Pages deployment
- Page has all 4 sections: hero, how it works, manifesto, getting started
- Dark theme with Bitcoin orange (#f7931a) accent throughout
- Terminal-style code blocks with getting started commands
- Responsive layout works on mobile (single column) and desktop (multi-column steps)
- Zero JavaScript — pure HTML + CSS per D-01/D-02
- CNAME configured for arbstr.com custom domain
- Links to both GitHub repos (arbstr and arbstr-node)
</success_criteria>

<output>
After completion, create `.planning/phases/25-landing-page/25-01-SUMMARY.md`
</output>
